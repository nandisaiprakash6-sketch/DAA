
# conclusion:-

'''This program calculates the factorial of a given number n using iteration.

Time Complexity: O(n) — because the loop executes n times.
Space Complexity: O(1) — because it uses only a constant amount of extra memory.
Approach: Iterative
Advantage: Uses less memory than the recursive factorial approach.
Final:

Time = O(n)
Space = O(1)

So, for factorial calculation, this iterative approach is more memory-efficient than the recursive approach.'''